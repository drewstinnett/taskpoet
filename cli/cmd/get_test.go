package cmd_test

/*
import (
	"bytes"
	"io/ioutil"
	"testing"

	"github.com/drewstinnett/taskpoet/cli/cmd"
)

func TestGet(t *testing.T) {
	getCmd := cmd.NewGetCmd()
	cmd := cmd.NewRootCmd()
	b := bytes.NewBufferString("")
	cmd.SetOut(b)
	cmd.AddCommand(getCmd)
	cmd.SetArgs([]string{"get"})
	cmd.Execute()
	out, err := ioutil.ReadAll(b)
	if err != nil {
		t.Error(err)
	}
	if string(out) != "Funkything" {
		t.Errorf("Expected 'Funky' but got %v", string(out))
	}
}
*/
